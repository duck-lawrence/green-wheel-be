import { LoginModal } from "./LoginModal"
import { RegisModal } from "./RegisModal"

export const Modals = () => {
    return (
        <>
            <LoginModal />
            <RegisModal />
        </>
    )
}
