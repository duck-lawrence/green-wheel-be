export const QUERY_KEYS = {
    ME: ["me"] as const,
    STATIONS: ["station"] as const,
    VEHICLE_SEGMENTS: ["vehicleSegment"] as const
    // POSTS: ["posts"] as const,
    // POST: (id: number) => ["post", id] as const
}
